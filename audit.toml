# cargo-audit configuration
# https://github.com/rustsec/rustsec/tree/main/cargo-audit

[advisories]
# Ignored advisories — all are transitive proc-macro deps of `discord-presence`
# v3.2.0 (compile-time only, not shipped in the binary) with no upstream fix
# available. Revisit when discord-presence releases an update or we migrate
# to an alternative Discord IPC crate.
ignore = [
    "RUSTSEC-2024-0436",  # paste 1.0.15 — unmaintained (proc-macro, via discord-presence)
    "RUSTSEC-2026-0173",  # proc-macro-error2 2.0.1 — unmaintained (via quork-proc -> discord-presence)
]
